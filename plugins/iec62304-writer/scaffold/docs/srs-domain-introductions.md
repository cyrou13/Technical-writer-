<!--
  Introductions of the functional areas of the SRS (§2.2.k).

  One `## <DOMAIN>` section per domain code of dt-config.yaml `domain_order`;
  the SRS exporter prints the section right under the area heading, before the
  area's requirements. An area without a section here gets no introduction.

  What belongs here: the clinical or technical context of the area, in a few
  sentences, and the literature it rests on, cited as [Rn] where Rn is an id of
  `references:` in dt-config.yaml (an unknown id is a release-lint error).
  What does not: requirements (they are items), methods (they are SDS items),
  code paths, dates, competitor names, research features.

  Edit by hand. English only.
-->

## ACQ

[TODO] The clinical or technical context of the area, in a few sentences, with its literature cited as [R1].
